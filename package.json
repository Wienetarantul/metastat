{
  "name": "metastat",
  "version": "0.1.0",
  "private": true,
  "description": "MetaStat — помощник по Dota 2 в Telegram",
  "type": "module",
  "scripts": {
    "bot": "node bot/index.js",
    "check": "node scripts/check-player.js",
    "app": "npm --prefix webapp run dev",
    "app:install": "npm --prefix webapp install",
    "app:build": "npm --prefix webapp run build"
  },
  "dependencies": {
    "grammy": "^1.30.0"
  },
  "engines": {
    "node": ">=20.12"
  }
}
